#ifndef ANIMAL
#define ANIMAL

#include "../machine/Code.h"
#include <vector>
#include <functional>

//JAVA TO C++ CONVERTER NOTE: Forward class declarations:
class Const;
class PrologMachine;

class animal {
public:
	static Const* s0;
	static Const* s1;
	static Const* s2;
	static Const* s3;
	static Const* s4;
	static Const* s5;
	static Const* s6;
	static Const* s7;
	static Const* s8;
	static Const* s9;
};

class pred_animal_1 : public Code {

	/*
	 * 
	 * animal(goat,_5346):-call(_5346). animal(elephant,_5420):-call(_5420).
	 * animal(_5472,_5514):-write('please type one more animal name:
	 * ',read(_5472,_5514)).
	 * 
	 * 
	 */

public:
	static* std::function<Function*(PrologMachine*);

	static* std::function<Function*(PrologMachine*);

	static* std::function<Function*(PrologMachine*);

	static* std::function<Function*(PrologMachine*);
};


#endif	//#ifndef ANIMAL
